import Box from '@mui/material/Box';

/**
 * Content section with optional spacing.
 * @param {import('@mui/material/Box').BoxProps} props
 */
export function Section(props) {
  return <Box component="section" {...props} />;
}
